[project]
name = "amfv"
version = "0.0.1"
description = "Shared workspace for the AMFV runtime components"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "amfv-datasets",
    "amfv-decomposer",
    "amfv-search",
    "amfv-utils",
    "amfv-verifier",
    "amfv-web-backend",
    "pytest>=8.0",
    "prek>=0.2.24,<1.0.0",
    "ruff>=0.11",
    "ty>=0.0.25",
]

[tool.uv]
package = false

[tool.uv.sources]
amfv-datasets = { workspace = true }
amfv-decomposer = { workspace = true }
amfv-search = { workspace = true }
amfv-utils = { workspace = true }
amfv-verifier = { workspace = true }
amfv-web-backend = { workspace = true }

[tool.uv.workspace]
members = [
    "datasets",
    "decomposer",
    "search",
    "utils",
    "verifier",
    "web/backend",
]
exclude = [
    "baseline",
    "training",
]

[tool.pytest.ini_options]
testpaths = [
    "datasets/test",
    "decomposer/test",
    "search/test",
    "utils/test",
    "verifier/test",
    "web/backend/tests",
]
